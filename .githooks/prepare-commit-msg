#!/bin/sh
# 移除 Cursor Agent 自动注入的 Co-authored-by 尾注
COMMIT_MSG_FILE=$1
[ -f "$COMMIT_MSG_FILE" ] || exit 0

tmp="${COMMIT_MSG_FILE}.tmp"
grep -v '^Co-authored-by: Cursor <cursoragent@cursor.com>$' "$COMMIT_MSG_FILE" > "$tmp" && mv "$tmp" "$COMMIT_MSG_FILE"
