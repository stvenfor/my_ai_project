name: module_supabase
description: Supabase infrastructure — Auth + Profile implementation.
publish_to: 'none'
version: 0.0.1

environment:
  sdk: '>=3.0.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  get: ^4.7.3
  http: ^1.2.2
  supabase_flutter: ^2.9.1
  module_core:
    path: ../../core

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
