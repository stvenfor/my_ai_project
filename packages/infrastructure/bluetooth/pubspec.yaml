name: module_bluetooth
description: Bluetooth LE infrastructure for flutter_module_sample.
publish_to: 'none'
version: 0.0.1

environment:
  sdk: '>=3.0.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_blue_plus: ^2.3.8
  get: ^4.7.3
  permission_handler: ^12.0.3
  module_common_ui:
    path: ../../commons/ui
  module_route:
    path: ../../route

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
