name: module_global_cache
description: Local storage for flutter_module_sample.
publish_to: 'none'
version: 0.0.1

environment:
  sdk: '>=3.0.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  json_annotation: ^4.12.0
  module_utils:
    path: ../toolkit
  path: ^1.9.1
  shared_preferences: ^2.5.5
  sqflite: ^2.4.2

dev_dependencies:
  build_runner: ^2.4.14
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  json_serializable: ^6.9.4

flutter:
