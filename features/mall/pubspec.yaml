name: module_mall
description: 门店商城 — 商品瀑布流与下单入口。
publish_to: 'none'
version: 0.0.1

environment:
  sdk: '>=3.0.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  get: ^4.7.3
  flutter_staggered_grid_view: ^0.7.0
  module_common_ui:
    path: ../../commons/ui
  module_core:
    path: ../../commons/core
  module_http:
    path: ../../commons/network
  module_utils:
    path: ../../commons/toolkit
  wys_router:
    path: ../../commons/wys_router
  module_auth:
    path: ../auth
  wys_common:
    path: ../../commons/wys_common

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

dependency_overrides:
  rxdart: ^0.27.7

flutter:
  uses-material-design: true
